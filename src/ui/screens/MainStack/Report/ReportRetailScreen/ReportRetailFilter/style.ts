import {StyleSheet} from 'react-native';

const ReportRetailFilterStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '100%',
  },
  date: {
    justifyContent: 'center',
  },
});
export {ReportRetailFilterStyle};
