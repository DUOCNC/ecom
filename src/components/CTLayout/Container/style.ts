import {Colors} from 'assets/colors';
import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.Background,
  },
  context: {flex: 1},
  bottom: {flex: 0, backgroundColor: Colors.White},
});

export default style;
