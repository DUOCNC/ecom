import {colors} from 'assets/v2';
import {StyleSheet} from 'react-native';

const CTTextStyle = StyleSheet.create({
  base: {
    color: colors.secondary.o900,
    fontFamily: 'Roboto',
  },
});

export {CTTextStyle};
