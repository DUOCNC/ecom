import {Colors} from 'assets/colors';
import {StyleSheet} from 'react-native';

const ViewFeatureStyle = StyleSheet.create({
  container: {
    backgroundColor: Colors.White,
    flex: 1,
    paddingTop: '10%',
  },
  center: {
    paddingTop: '50%',
  },
});

export default ViewFeatureStyle;
