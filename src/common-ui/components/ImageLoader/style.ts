import {StyleSheet} from 'react-native';

const CTImageStyle = StyleSheet.create({
  placeholder: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});

export default CTImageStyle;
