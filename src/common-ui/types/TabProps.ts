import {LayoutProps} from './LayoutProps';

/**
 * TabProps
 * Using for Layout.Tab
 * Version 1
 */
export interface TabProps extends LayoutProps {
  barStyle: 'default' | 'light-content' | 'dark-content';
}
