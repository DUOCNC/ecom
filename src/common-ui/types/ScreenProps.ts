import {LayoutProps} from './LayoutProps';

/**
 * ScreenProps
 * Using for Layout.Screen
 * Version 1
 */
export interface ScreenProps extends LayoutProps {
  barStyle: 'default' | 'light-content' | 'dark-content';
}
