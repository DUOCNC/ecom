export enum MainErrorType {
  TIMEOUT = 'Timeout',
  SearchNotfound = 'SearchNotfound',
  ServerError = 'Error',
  NotPermission = 'NotPermission',
}

export enum AlignVerticalErrorEnum {
  top = 'top',
  center = 'center',
}
